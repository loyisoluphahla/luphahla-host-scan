services:
  - type: web
    name: luphahla-bugscan
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python render_app.py
    healthCheckPath: /healthz
    autoDeploy: true
